<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.andk.intime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InTime">

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddActivity"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".EditActivity"
            android:screenOrientation="portrait"></activity>

        <receiver android:name=".DailyReceiver" />
        <receiver android:name=".СheckingReceiver"/>
        <receiver android:name=".FifteenMinuteNotificationReceiver"/>
        <receiver android:name=".ZeroMinuteNotificationReceiver"/>
    </application>

</manifest>